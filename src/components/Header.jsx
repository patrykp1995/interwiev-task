import React from "react";

const Header = () => {
  return (
    <header className="App-header">
      <span>
        <strong>ForeCast</strong> WeatherApp
      </span>
    </header>
  );
};

export default Header;
